{
    "name": "reservation-calendar",
    "version": "1.0.0",
    "description": "This project is completed as a Job hiring task of Stellic.",
    "main": "index.js",
    "scripts": {
        "postinstall": "npm run copy-libs",
        "update-deps": "npm update",
        "postupdate-deps": "npm run copy-libs",
        "copy-libs": "cpx \"node_modules/{angular,angular-*,bootstrap/dist}/**/*\" app/lib -C",
        "prestart": "npm install",
        "start": "http-server ./app -p 8000 -c-1 -o ",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Atif Mehmood",
    "license": "ISC",
    "dependencies": {
        "angular": "^1.4.14",
        "bootstrap": "^4.5.0"
    },
    "devDependencies": {
        "cpx": "^1.5.0",
        "http-server": "^0.12.3"
    }
}
